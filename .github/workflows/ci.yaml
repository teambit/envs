name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: install
      run: |
        npm config set @bit:registry https://node.bit.dev
        npm i 

    - name: run tests
      run: npm run test